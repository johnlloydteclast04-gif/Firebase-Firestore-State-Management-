# Firebase-Firestore-State-Management-
I created the project in the Firebase Console and added my app platform. The Firebase provided a configuration file, which I copy or added to my project. Then I installed the required Firebase dependencies and initialized the Firebase in the main file of the app. 
